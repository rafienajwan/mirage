# Browser-visible gateway URL
NEXT_PUBLIC_API_URL=http://localhost:8000

# Gateway
APP_ENV=development
DATABASE_URL=
FRONTEND_ORIGIN=http://localhost:3000
RISK_THRESHOLD=60
ANOMALY_REDIRECT_CONFIDENCE=0.5
RATE_LIMIT_PER_MINUTE=120
REAL_APP_URL=http://localhost:8001
DECOY_SERVICE_URL=http://localhost:8002
PROXY_TIMEOUT_SECONDS=10
PROXY_MAX_BODY_BYTES=1048576
# MIRAGE_API_KEY=replace-with-a-long-random-value

# Docker Compose / PostgreSQL
POSTGRES_USER=mirage
# Required. Use a strong, URL-safe value for local Compose.
POSTGRES_PASSWORD=
POSTGRES_DB=mirage
